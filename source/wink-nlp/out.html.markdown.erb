# Leveraging .out()

In order to understand its versatility, let us explore how it works on an item and on collection.

## item.out()

As we have seen in previous examples, by default it returns the value of the item as Javascript string. By using `.out()` you can access any property or view of the item.

The `.out()` produces output in javascript built-in datatypes intelligently depending on the usage. It is available universally at all levels — document, collect, and item. It is one of the most versatile API methods; for its in depth coverage, refer to the section on “Leveraging .out()”.
